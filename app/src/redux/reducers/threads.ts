import { ADD_THREAD, DELETE_THREAD, ADD_REACTION, REMOVE_REACTION, ADD_COMMENT, REMOVE_COMMENT } from "../actionTypes";

const initialState = {};

export default function Threads(state: any = initialState, action: any) {
switch (action.type) {
    case ADD_THREAD: {
        return 
    }
    case DELETE_THREAD:{
        
    }
    case ADD_REACTION:{
        
    }
    case REMOVE_REACTION:{
        
    }
    case ADD_COMMENT:{
        
    }
    case REMOVE_COMMENT:{
        
    }
    default: return state;
}